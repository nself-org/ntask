name: Lighthouse CI

# Disabled: Next.js web target removed in commit a3ed9c4 (Flutter is now canonical).
# Re-enable when Flutter web is set up with a hosted preview URL.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Lighthouse CI (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lighthouse CI not applicable to Flutter app."
