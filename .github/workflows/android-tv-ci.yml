name: Android TV CI

# Disabled: Capacitor Android TV build removed with frontend/ in commit a3ed9c4.
# Flutter Android TV support TBD.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Android TV CI (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android TV support not yet implemented in Flutter."
