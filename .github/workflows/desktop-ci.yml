name: Desktop CI

# Disabled: Tauri/Next.js desktop removed in commit a3ed9c4 (Flutter is now canonical).
# Flutter desktop builds are handled by flutter-ci.yml.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Desktop CI (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Flutter desktop builds handled by flutter-ci.yml."
